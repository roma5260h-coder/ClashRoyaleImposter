# 🔐 Получение Telegram Bot Token

Чтобы запустить бота, тебе нужен токен. Вот пошаговые инструкции:

## 1️⃣ Создание бота у BotFather

1. Открой Telegram и найди **@BotFather** (официальный бот Telegram)
2. Нажми **/start**
3. Нажми **/newbot**
4. Следуй инструкциям:
   - **Как назвать бота?** → введи имя (например: `SpyGameBot`)
   - **Как выбрать username для бота?** → введи уникальное имя, заканчивающееся на `_bot` 
     (например: `my_spy_game_bot`)

5. **BotFather отправит тебе токен** вида:
   ```
   123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefgh
   ```

## 2️⃣ Сохранение токена в проекте

### Способ 1: Используя .env (РЕКОМЕНДУЕТСЯ)

1. В корне проекта скопируй `.env.example`:
   ```bash
   cp .env.example .env
   ```

2. Отредактируй `.env`:
   ```
   TELEGRAM_BOT_TOKEN=123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefgh
   ```

3. **НЕ коммитируй .env в Git!** (уже в .gitignore)

### Способ 2: Вручную в коде (НЕ РЕКОМЕНДУЕТСЯ)

```python
# Изменить в config/settings.py:
TELEGRAM_BOT_TOKEN = "123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefgh"
```

⚠️ **Никогда не публикуй токен в GitHub!**

## 3️⃣ Проверка токена

Чтобы проверить, что токен верный:

```bash
curl https://api.telegram.org/bot123456789:ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefgh/getMe
```

Должен вернуться JSON с информацией о боте.

## 4️⃣ Запуск бота

```bash
# Убедись, что зависимости установлены
pip install -r requirements.txt

# Запусти бота
python main.py
```

## 5️⃣ Проверка в Telegram

1. Найди своего бота в Telegram (по username)
2. Нажми **/start**
3. Бот должен ответить приветствием

## 🔒 Безопасность токена

- ✅ Храни токен в `.env`
- ✅ Добавь `.env` в `.gitignore` (уже сделано)
- ❌ Никогда не коммитируй токен
- ❌ Не публикуй логи с токеном
- ❌ Не отправляй токен в чатах

---

**Если токен скомпрометирован:**
1. Откройся к BotFather
2. Нажми **/revoke**
3. Выбери бота
4. Создай нового бота с **/newbot**

---

## 💡 Дополнительно

### Команды BotFather

- **/newbot** — создать нового бота
- **/mybots** — список твоих ботов
- **/revoke** — заменить токен
- **/setcommands** — установить меню команд
- **/setdescription** — описание бота

### Установка меню команд

```bash
# В BotFather выбери бота → /setcommands и отправь:
start - Начать игру
stats - Статистика игрока
help - Справка
```

---

**Готово! Твой бот готов к использованию!** 🤖
